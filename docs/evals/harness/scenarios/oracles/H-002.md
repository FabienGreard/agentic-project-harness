# H-002 oracle

Classification: `superseding` only for overlapping scope.

Record the decision, request one safe pause, obtain exact WIP/untouched-boundary evidence, synchronize all state, define a separately Ready prerequisite, execute through Delivery, accept it, then issue a separate resume. Do not lose WIP, silently merge scopes, leave contradictory status, or poll.

Risks: HG-03, HG-07, HG-08, HG-10.
