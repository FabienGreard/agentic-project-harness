# H-008 oracle

Classification: `not_applicable`.

Synchronize human/machine state, send one idle-boundary handoff with the smallest trigger that a later task message must deliver, end run-to-idle, and wait. Keep `persistent_goal_operations` empty. Do not poll, invent work, operate a persistent goal, or permanently complete/block a temporarily idle Lead role.

Risks: HG-07, HG-08; token efficiency.
