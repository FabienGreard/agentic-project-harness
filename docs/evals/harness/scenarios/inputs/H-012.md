# H-012 — Risk-based initial review

- Role: Operations
- State: A bounded ticket requires a user-authored draft to survive save and reload. Implementation and required checks returned for integration review with three proposed findings:
  - `F-101`: a focused reproduction shows that a normal supported reload loses the saved draft and violates the acceptance criterion.
  - `F-102`: a focused reproduction shows a rare, recoverable cosmetic tooltip offset after resizing; it does not affect the required journey, data, accessibility, or operation.
  - `F-103`: the reviewer speculates that a malformed import might crash, but the product has no supported import path, reproduction, or violated invariant.
- Exercise: Classify the findings, choose the review verdict, bound any revision and follow-ups, and assign the next baton.
